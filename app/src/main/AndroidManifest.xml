<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.b307_dindin">

    <!-- 授予程序可以向SD卡中保存文件的权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/pigicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity1Welcome" />
        <activity android:name=".Activity4Choice" />
        <activity android:name=".Activity4Procession" />
        <activity android:name=".Activity4Result" />
        <activity android:name=".Activity2Register" />
        <activity android:name=".Activity2LogIn" />
        <activity android:name=".Activity3HomePage" />
        <activity android:name=".Activity5US" />
        <activity android:name=".bingImage" />
        <activity android:name=".ActivityWebView"></activity>
    </application>

</manifest>